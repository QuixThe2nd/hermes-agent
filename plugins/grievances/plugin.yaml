name: grievances
default_enabled: true
version: 1.0.0
description: "Agent files structured grievances (things it wants the user to know, not auto-resolvable bugs) to a self-provisioned Discord channel."
author: Hermes Agent
kind: standalone
provides_tools:
  - grievances
